[package]
name = "lau-kintsugi"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-kintsugi"
description = "Rust library for kintsugi in the PLATO ecosystem"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
